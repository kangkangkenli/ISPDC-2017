1. The entry function file is main.
   In function main, there is a variable buffer_size, which is 
   the depth of scan window. buffer_size can be set as 100, 500, 1000

2. The function file data generates workload of jobs with node requests which has small jobs only, small + medium jobs, and small + medium + large jobs.

3. File trace.csv is the originally Blue Waters trace of Sep. 2015.
